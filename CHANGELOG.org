#+title: Changelog

** 0.3.1

Polish + edit-mode rework on top of v0.3.0.

*** macOS task inspector — explicit edit mode

The card no longer drops you into title/notes editing on tap. Edit mode is
opt-in via the new =Edit= action in the row or card context menu. Reading
and editing are now separate states.

- Title is read-only by default; entering edit mode shows the =TextField= and reveals a footer pair of =Discard= (revert original notes + title) and =Save= (=Enter= via =.defaultAction=)
- =originalNotes= / =originalTitle= snapshots taken at load so =Discard= reliably reverts even after multiple in-card edits
- Removed the implicit "tap notes to edit" gesture (=NotesRenderedView= no longer steals taps)
- Removed the redundant =×= close button in the top-right; clicking the title, pressing =Esc=, or clicking outside still dismisses
- Card has its own context menu mirroring the row actions (state / priority / schedule / deadline / clock / refile)

*** Outline breadcrumb in the inspector

A new monospaced =file ▸ Heading ▸ Heading= line under the title row shows where the heading lives. Backed by a new =GET /api/outline?file=&pos== that returns =org-get-outline-path= over the heading's ancestor chain.

- =eav-get-outline-path= elisp helper visits the heading inside =org-with-wide-buffer= and abbreviates the file path (=~/= replaces =$HOME=)
- =APIClient.fetchOutlinePath= + =OutlinePathResponse=; the inspector loads it on appear and on task switch, race-guarded against task changes mid-flight

*** macOS task row — clickable state and priority

The state pill and priority badge are now buttons that open menus directly on the row, no inspector required:

- =stateMenu= reads =keywords= passed in from the parent view and renders =Active= / =Done= sections plus a =Clear= action; arbitrary user TODO sequences flow through unchanged
- =priorityMenu= offers A/B/C/D and =None=
- =keywords: TodoKeywords?= threaded through =MacTaskRow= from =MacTodayView=, =MacUpcomingView=, =MacAllTasksView=, =MacEisenhowerView=, =GroupSection=
- Right-click on a row gains an =Edit= item that calls a new =editInspector= action — opens the card in edit mode in one click

*** macOS Upcoming view — proper day head

Day groups got the same treatment as Today's =dayHead=:

- =TODAY= / =TOMORROW= / =IN N DAYS= / =NEXT WEEK= / =IN N WEEKS= / =IN A MONTH= / =IN N MONTHS= overline, color-graded by proximity
- 22pt bold full date label (=EEEE, MMM d=) with a =N tasks · M events= subtitle

*** Hierarchically-weighted checklist progress

=ChecklistProgress.compute= now builds a tree from indent levels and computes done/ongoing as recursive averages instead of flat counts.

- Avoids double-counting parent items that org auto-aggregates to =[-]=: for the canonical =- [X] A / - [ ] B / - [-] C { - [X] / - [-] }= example the result is =done = 1/2=, =ongoing = 1/6= instead of the old flat =2/5= / =2/5=
- Return type changed from =(done: Int, ongoing: Int, total: Int)= to a =ChecklistProgress= struct with =Double= fractions in =[0, 1]= plus =itemCount= for "% complete" tooltips
- Call sites in =MacTaskRow= and =TaskExpandedCard= use the fractions directly

*** macOS clock dock — robust priority lookup

=priorityFor= now matches a session by id *or* file+title:

- The session id is a =file::pos= snapshot taken at clock-in; as CLOCK lines accumulate in the LOGBOOK drawer the heading's =pos= shifts and the id goes stale. Matching by =(file, title)= as a fallback survives that drift
- Generic =find<T: TaskDisplayable>= helper folds the three identical lookups (=allTasks=, =today=, =upcoming=) into one
- Cosmetic: =⏱= → =⏰= so the dock icon reads at the system size

*** Sidebar dismisses inspector

Clicks anywhere in the =NavigationSplitView= sidebar (rows or empty space) now clear =taskSelection.taskId= via =simultaneousGesture(TapGesture)=. Plain =onTapGesture= wasn't reaching the =NSTableView=-backed sidebar items on macOS.

** 0.3.0

Major macOS design refresh based on a Sonoma-vibrancy mockup from claude.ai/design,
plus several reliability fixes uncovered along the way.

*** macOS task row layout

- Single-line row: =[State] [Priority] Title [📅 Scheduled] [⚑ Deadline] [⏱ Clocked] —spacer— [#tag #tag] [CATEGORY]=
- =PriorityBadge= — solid 16×16 colored square with white mono letter (A red · B orange · C blue · D gray); replaces the old flag-and-letter badge
- =DateBadge= — scheduled is a blue tinted pill with calendar icon; deadline is =flag.fill= with severity-tinted bg + border (overdue red, soon orange, normal gray)
- Tags render as outlined mono =#tag= chips (right-aligned) and the category renders as a solid uppercase pill
- TODO state pills use per-state hues parsed from =org-todo-keywords= (TODO blue, NEXT green, PROJ purple, ACTV orange, WAIT/KILL gray)
- Removed inter-row dividers; all-day events use a quieter single-line banner with leading color bar, monospaced time, and lowercase calendar tag

*** macOS Today view

- New day-head: =TODAY= overline + 22pt bold day label + =N tasks · M events= subtitle
- =SCHEDULE= section interleaves timed events and timed tasks chronologically by minute-of-day; previously they were lumped into a separate top block, breaking time order
- All-day events surface above the schedule under an =ALL DAY= label
- Untimed tasks group below by category as before

*** macOS task inspector (=TaskExpandedCard=)

Replaces the row when selected — clean Things-3-style card, no surrounding border, soft 14pt shadow:

- Title is always editable (commits on Enter / blur via =saveTitle=)
- TODO state pill, priority box, scheduled chip, and deadline chip in the title row are all clickable; each opens a Button-driven popover (not a =Menu= — =.menuStyle(.borderlessButton)= was overriding the white text in the priority box label)
- Notes render by default with interactive checkboxes; tapping anywhere in the notes area enters raw-text edit mode (=TextEditor= focuses automatically); blur saves and switches back
- Footer row: =[★ Scheduled] —spacer— [▶ clock] [# tags] [≡ state/priority] [⚑ deadline] [📥 refile]=
- Tags editor — popover with comma-separated text field, inherited tags listed read-only
- Refile triggers the existing =RefileSheet=
- Clock toggle: =play.circle= / =stop.circle.fill= (replaces the old separate clock chip that disappeared in the row redesign)
- Dismiss via Esc, the × in the top-right, or clicking anywhere outside the card
- Checklist progress visible as a 3pt strip on the top edge of the card (faint gray base, green for done, muted orange for ongoing)

*** Things-3-style date picker (=DatePickerPopover=)

- Dark compact popover replaces the heavy =.graphical= =DatePicker=
- =When= text field at top — type =today=, =tomorrow=, =next week= + Enter to jump
- Quick rows: =★ Today=, =🌙 This Evening= (today @ 18:00), =📥 Someday= (clears)
- Compact month grid with prev/next chevrons; today is a gold star, the selected day fills a colored circle in the chip's tint
- Inline =🕒 Time= row with =Add time= → embedded =HH:MM= picker; =×= removes the time. Time edits commit immediately and keep the popover open so you can keep tweaking; day clicks commit and dismiss
- =Clear= strip at the bottom

*** Multi-clock dock (=MacClockDock=)

- Each running session as its own row: =⏱ [Priority] Title —spacer— [CATEGORY] [elapsed] [stop] [×]=
- Per-row priority badge looked up from =store.allTasks/today/upcoming= so it matches the row palette
- Pulsing 3pt orange edge bar (=Theme.priorityB=) via =TimelineView=, plus a faint orange→clear horizontal gradient over the dock surface
- Removed the =CLOCKED IN= header pill (the orange tint already conveys that state)

*** Row highlight + checklist progress settings

Three new settings (Settings → Row Highlight, Settings → Checklist Progress):

- *Highlight by* — =Off=, =Priority=, or =TODO State= — colors come from =settings.resolvedPriorityColor/resolvedTodoStateColor= so user-customized hexes flow through automatically
- *Highlight style* — =Edge bar= (3pt left strip) or =Background tint= (subtle row fill)
- *Progress style* — =Top line= (2pt strip), =Circle= (donut at row end), =Background fill= (left-to-right green for done, muted orange for in-progress, clipped to the row's rounded corners)

*** Notes parsing + clock data

- =NotesParser= already skipped =:LOGBOOK:= drawers; the inspector hides them too
- Fixed bug in =eav-add-clock-entry=: when a =:LOGBOOK:= drawer existed, the new CLOCK line was being inserted *after* =:END:= instead of inside the drawer (cause: =org-end-of-meta-data= jumps past the drawer; the old code did =forward-line 1= from there). New behavior: jump to the =:LOGBOOK:= header, =forward-line 1=, insert.
- =eav-clock-in= / =eav-clock-out= bind =org-clock-into-drawer "LOGBOOK"= so their CLOCK lines always land inside the drawer regardless of the user's personal config
- New =eav-tidy-clocks= elisp + =POST /api/clock/tidy= server route + =store.tidyClocks= → folds any pre-existing loose =CLOCK:= lines under a heading into a =:LOGBOOK:= drawer
- =eav--collect-loose-clock-lines= helper sweeps loose lines while preserving entry-end positioning via a marker
- Inspector shows an orange =N loose CLOCK entries outside :LOGBOOK:= banner with a =Tidy= button when legacy loose entries are detected

*** =org-todo-list=-correct task filtering

- =eav-extract-all-tasks= was passing =nil= as the match argument to =org-map-entries=, walking every heading. Fixed: only headings with =(org-get-todo-state)= are included.
- =eav-extract-active-tasks= had a similar bug — and was using a hardcoded =("DONE" "KILL" "[X]")= list. Now uses a new =eav--done-keywords= helper that parses =org-todo-keywords= (everything after the =|= per sequence, stripping =(c)= shortcut hints), making it correct for arbitrary user TODO sequences.
- =eav-extract-tasks-for-file= gets the same TODO-state guard.

*** Live progress + observation

- =TasksStore.prefetchNotes= now always re-fetches on row appearance (only the in-flight de-dup set short-circuits) so the row's progress reflects live state even when notes are edited out-of-band in Emacs
- =TasksStore.notesCacheRevision= bumps on every cache mutation; =RowActionFactory.progress(for:)= reads it explicitly so the @Observable framework reliably tracks dictionary mutations through the helper-closure boundary

*** Click-outside / dismiss UX

- Escape key, the × in the inspector top-right, and clicks anywhere outside the card all dismiss the inspector
- Dismiss handler moved from the =ScrollView=' s =.background= (which doesn't extend into scrollable content) to the =LazyVStack=' s background with =minHeight: 600=, so empty space inside the scroll content also dismisses
- Wired in Today / Upcoming / All Tasks / Eisenhower

*** Calendar sidebar regression fix

- Switching to the Calendar view dropped the sidebar's icons + Filters section + footer because the =RootView=' s =HSplitView { mainPane | MacScheduleTray }= nested badly inside the outer =NavigationSplitView=. Moved the schedule-tray split inside =MacCalendarView= as an =HStack { calendarPane | MacScheduleTray }= with a fixed 340pt tray. The detail area is now a single column for every view.

*** Tags edit + APIs

- =APIClient.setTags= + =store.setTags= + =MacRowActionsBuilder.setTags= wired through to the existing =PATCH /api/tasks/:id/tags= endpoint
- =DatePickerPopover= callback signature changed from =(Date, Bool) -> Void= to =(Date, Bool, _ closing: Bool) -> Void= so callers can keep the popover open while inline-editing the time

** 0.2.1

- macOS app: replace three-column inspector layout with inline task expansion
  - =TaskExpandedCard= renders notes, checklists, TODO state/priority pickers, scheduling/deadline date pickers with time toggle, and clock controls directly below the selected row
  - =NotesRenderer= parses org notes into blocks (checklists, bullets, paragraphs), skips =:PROPERTIES:=/=:LOGBOOK:= drawers, renders inline org markup (bold, italic, code, links)
  - =RootView= drops the three-column =NavigationSplitView= (sidebar/content/inspector) in favour of a two-column layout; calendar view uses =HSplitView= to dock the schedule tray alongside the grid
  - =MacScheduleTray= removes the =VSplitView= inspector pane; tapping a row toggles an inline =TaskExpandedCard= instead
  - =GroupSection= renders =TaskExpandedCard= inline below the selected row in Today/Upcoming/All Tasks views
  - Context menus simplified: removed "Pick Date…" and "Edit in Inspector" items that relied on the inspector panel
- macOS app: calendar grid visual overhaul
  - Navigation controls moved from toolbar to an inline header bar with compact segmented range picker
  - Dedicated day-header row with weekday abbreviation + circled day number (accent capsule for today)
  - Column separators (vertical rules) between days in both the all-day strip and timed grid
  - All-day strip uses deterministic row height, limits visible rows, shows "+N more" overflow button
  - Hours gutter refactored for correct label alignment with hour rules
  - Initial scroll shows two hours of past context instead of landing on the current hour
- macOS app: clock-done integration — completing a task (via =toggleDone= or =setState=) automatically stops and logs any running clock for that task
- macOS app: =ClockManager= fixes race condition on =stop()= — re-resolves array index after =await= to prevent out-of-bounds crash from concurrent mutations
- macOS app: list views (Today/Upcoming/All Tasks) use full available width instead of capping at 880px

** 0.2.0

- Native macOS / iOS app (=apps/macos/=, SwiftUI, two targets sharing one codebase)
  - Things 3-style NavigationSplitView with sidebar, content list, and resizable inspector
  - Today / Upcoming / All Tasks / Calendar views with sort, group, search, and collapsible group sections
  - Calendar grid (Day/Week, 0-24h with half-hour markers), lane layout for overlapping events, drag-to-reschedule with live snap preview, drag-to-resize that writes single-day time-range timestamps =<2026-04-21 Tue 14:00-15:30>=
  - Scheduling tray bound to the calendar's anchor day, with range picker (Day / 7 Days / Month), "include deadlines" + "exclude scheduled" toggles, search, and a bottom-half inspector for the selected task
  - Multi-clock dock: run several clocks simultaneously; stop pushes a =CLOCK: [start]--[end] => H:MM= line into the task's =LOGBOOK= drawer; sessions persist across app restarts via UserDefaults
  - EventKit two-way sync: "Add to Calendar" on an org task creates an =EKEvent= and stores its external id in a =:GCAL_EVENT_ID:= property; changes pushed via =EKEventStoreChanged= notifications. Inline event creation (⌘N or double-click empty slot) and unified rendering of org tasks + EKEvents on the calendar grid
  - Per-server category color overrides (Settings → Category Colors); auto-assigned palette via stable string hash for unset categories
  - Light / Dark / System theme, persisted; per-view sort/group defaults
- Backend additions to support the apps
  - =POST /api/clock/log= + elisp =eav-add-clock-entry= insert a completed =CLOCK= line into a task's =LOGBOOK= drawer (multi-clock support without disturbing the active org clock)
  - =PATCH /api/tasks/:id/property= + elisp =eav-set-property= write arbitrary org properties (used for =GCAL_EVENT_ID=)
  - Task extraction now exposes the heading's =properties= alist
- Mutation reliability
  - Express server auto-recovers after =eav.el= is reloaded — =withReloadRetry= in =server/emacs.ts= detects "Symbol's function definition is void" errors, re-loads =eav.el=, and retries once

** 0.1.14

- Upcoming view: sort date groups chronologically
  - Date groups inherited their order from =items=, which is pre-sorted by the active sortKey (priority/category/etc.). A =Map= preserves insertion order, so a high-priority Friday entry would push Friday's group ahead of Thursday's. Fix sorts the grouped pairs by the YYYY-MM-DD key before rendering.

** 0.1.13

- Classify =block= agenda entries as events, not TODOs
  - org-agenda emits =block= for ranged =<a>--<b>= timestamps (e.g. a calendar event that spans hours or days). Previously =isEventEntry= only matched =timestamp= and =sexp=, so block-type events fell through to the TODO list with empty state/priority pills. Now they route to =EventBanners= alongside the other calendar-style entries.

** 0.1.12

- Render active timestamps in notes as formatted chips instead of raw =<...>= text
  - Backend =eav--parse-timestamp= now delegates to =org-element-timestamp-parser=, returning structured start/end components, range type, repeater, and warning fields
  - =<a>--<b>= ranges (e.g. Game Night spanning midnight) come back as a single entry with both endpoints -- not two separate timestamps
  - =eav-get-heading-notes= also returns parsed =activeTimestamps= so the frontend can render chips by looking up each =raw= occurrence
  - =NotesRenderer= swaps each =raw= string in the body with a =TimestampChip= that formats via the browser's locale
- Test infrastructure
  - Elisp fixture-based tests (=elisp/tests/fixtures/{calendar-events,nested-lists}.org=) covering every timestamp flavor and every checklist bullet type (=-=, =+=, =*=, =1.=, =1)=, mixed, three-level). =npm test= runs 33 ERT tests
  - Playwright end-to-end tests (=npm run test:e2e=) on desktop Chromium and mobile WebKit (iPhone 13) that exercise the Game Night rendering fix against a real browser with mocked =/api/*=

** 0.1.11

- Nested org checklists render with hierarchy (indentation per nesting level)
  - =NotesRenderer= tracks each item's source indent and maps distinct indents to depths per block
- Parent checkbox states aggregate from descendants, matching org's native behavior
  - After saving notes, elisp helper =eav--propagate-checkbox-parents= runs =org-list-struct-fix-box= on each list in the heading body so parents become =[X]= when all children are checked, =[-]= when some are checked or in progress
  - =eav-set-heading-notes= now returns the propagated notes; the UI updates local state with the server's final version

** 0.1.10

- Fix checklist parsing for all org bullet types
  - =+= and =*= bullets now parsed as checklist items (not just =-=)
  - Ordered bullets (=1.=, =a)=) with checkboxes also supported
  - Sub-items (=-= lines under a =+ [ ]= item) grouped under their parent
  - Multi-line checklist items render sub-text indented below
  - Checkbox toggle works for all bullet types

** 0.1.9

- Persist sort, group-by, and show-done settings in =localStorage=
  - Restored on page load so preferences survive refresh

** 0.1.8

- Mobile UX improvements
  - Long-press (500ms) on tasks opens context menu (replaces right-click on mobile)
  - Increased row padding, pill sizes, and font sizes on mobile for better touch targets
  - =select-none= prevents text selection during long-press

** 0.1.7

- Fix false "Connection Error" screen on transient emacsclient failures
  - Clock status polling is now best-effort; failure doesn't take down the app
  - After initial load, refresh failures preserve existing data instead of showing error screen
  - Fixed infinite retry loop caused by =error= in =useCallback= dependency array
- Date picker auto-confirms when time input loses focus (no need to press the confirm button)
- Mutation failures (stale positions) now trigger a data refresh for recovery

** 0.1.6

- Right-click context menu on tasks with "Edit title" and "Refile" options
  - Edit title: inline text input using org's =org-edit-headline= (preserves TODO state, priority, tags)
  - Refile: searchable modal with all =org-refile-get-targets= headings, type to filter
  - Uses org's own =org-refile= with =rfloc= parameter for correct refile behavior

** 0.1.5

- Fix capture: use =cl-letf= to advise =org-capture--position-cursor= instead of string replacement
  - Lets org's full template expansion pipeline run unmodified
  - Title is inserted at =%?= position by the advised cursor function
- Optional priority, scheduled date, and deadline when capturing
  - Inline pickers in capture form (priority A-D, date with quick options + time)
  - Applied to the captured entry via =org-capture-last-stored-marker= after finalize

** 0.1.4

- Org capture from the web UI
  - Reads =org-capture-templates= from Emacs and presents them in a modal (=Cmd+N= or sidebar =+= button)
  - Supports =entry= type templates with static file/headline targets
  - Templates with function targets or bodies shown as "Emacs only"
  - Runs org-capture non-interactively with =:immediate-finish=; all =%u=, =%t=, =%<...>= expansions handled by org
  - "+" button in sidebar header for quick access

** 0.1.3

- Secondary sort by scheduled/deadline time within primary sort groups
  - When sorting by priority, tasks with the same priority are sub-sorted by their scheduled time
  - Works across all sort modes (priority, state, category, deadline)

** 0.1.2

- Mobile-friendly responsive layout
  - Sidebar becomes a slide-over drawer with backdrop on small screens
  - Header controls (sort, group) wrap to a second row on narrow viewports
  - Reduced padding and expanded-panel indent on mobile
  - Metadata rows and date pickers wrap gracefully
- Production server mode: Express serves built frontend (=npm start=)
  - Single-process deployment on port 3001, no separate Vite dev server needed
- launchd service plist for running as a background macOS service
- Safe-area / notch support for iOS devices (=viewport-fit=cover=, =env(safe-area-inset-*)=)
- Use =100dvh= for proper mobile browser viewport height
- Touch-action: manipulation on buttons to eliminate tap delay
- Fix Express 5 catch-all route syntax (=*= -> =/{*path}=) for production static serving

** 0.1.1

- Fix checklist rendering: support all three org checkbox states (=[ ]=, =[-]=, =[X]=)
- In-progress =[-]= items render as orange checkboxes with a dash, italic text
- Progress bar shows orange segment for in-progress items alongside green for checked
- Clicking a checkbox cycles: unchecked -> in-progress -> checked -> unchecked

** 0.1

Initial feature-complete release.

- Today/Upcoming/All Tasks views using org-agenda's own machinery
- Clickable TODO states, priorities, scheduled/deadline dates with calendar+time picker
- Inline notes viewing/editing with interactive org checklists
- Org clocking (clock-in, clock-out, live timer banner)
- Multi-level grouping (agenda type, priority, category, state) with collapsible headers
- Calendar event banners, org markup rendering (bold, italic, code, links)
- Light/dark/auto theme switching with proper CSS variable system
- Collapsible sidebar with keyboard shortcut (=Cmd+\=)
